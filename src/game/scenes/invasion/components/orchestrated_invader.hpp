#pragma once

namespace components {

// * move every X ticks, adjusted by remaining aliens
// * reverse and drop down on hitting wall
// * reset aliens when all are gone
// * animate on movement

struct OrchestratedInvader {};

} // namespace components